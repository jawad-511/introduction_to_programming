#include <iostream> 
#include <cmath>
using namespace std;

int main() {

    short number;
    cout << "Enter number: " << endl;
    cin >> number;

    cout << endl << endl<< pow(number , 2) << endl << pow(number , 3) << endl << pow(number , 4);
    return 0;
}